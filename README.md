# Crop Intelligence Dashboard

One-page crop-weather intelligence demo with daily forecast ingestion, ERA5-informed horizon extension, and a durable DuckDB archive stored outside the serverless filesystem.

Secrets are configured only through deployment environment variables and are never exposed to browser code.
